// Area contract: task subsystem entrypoint for async execution and MCP task handlers.
// Export only task-facing APIs consumed outside tasks/; keep tool logic and transport-specific helpers out.

export * from './manager.js';
